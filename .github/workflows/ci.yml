name: CI
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Install dependencies
        run: |
          # Install required dependencies
          sudo apt update
          sudo apt install -y gcc
      - name: Build
        run: |
          # Code to build the project goes here
          # Replace this with actual build command
          echo 'Building trend-audit-agent...'
